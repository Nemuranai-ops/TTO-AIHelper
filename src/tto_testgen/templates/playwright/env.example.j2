# Generated by TAAS. Every variable the project reads appears here, with a
# description and no value. A project whose configuration is discoverable only by
# running it and reading the crash is one the engineer must reverse-engineer first.

# Base URL of the application under test. Required.
TAAS_BASE_URL=

# Credentials for the shared authentication fixture. Required if any test signs in.
TAAS_AUTH_USERNAME=
TAAS_AUTH_PASSWORD=

# Base URL for API tests. Defaults to TAAS_BASE_URL when unset.
TAAS_API_BASE_URL=

# Timeouts in milliseconds. Optional; the defaults are in playwright.config.ts.
TAAS_ACTION_TIMEOUT_MS=
TAAS_NAVIGATION_TIMEOUT_MS=
TAAS_EXPECT_TIMEOUT_MS=
{% for name in extra_variables %}

# Declared by the corpus.
{{ name }}=
{% endfor %}
