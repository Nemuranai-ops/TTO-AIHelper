// Generated by TAAS from the UI model. Locators are defined centrally here and not
// repeated across specs (FR-AUT-02).
import { type Page, type Locator } from '@playwright/test';

export class {{ class_name }} {
  readonly page: Page;
{% for element in elements %}
{% if element.annotations %}
{% for note in element.annotations %}
  // {{ note }}
{% endfor %}
{% endif %}
  readonly {{ element.property }}: Locator;
{% endfor %}

  constructor(page: Page) {
    this.page = page;
{% for element in elements %}
    this.{{ element.property }} = page.{{ element.expression }};
{% endfor %}
  }
{% if route %}

  async goto(): Promise<void> {
    await this.page.goto({{ route | ts }});
  }
{% endif %}
}
