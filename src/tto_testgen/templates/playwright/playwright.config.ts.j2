// Generated by TAAS. Safe to edit: this file is never overwritten once changed -
// a regeneration detects the edit and reports the file as stale instead.
import { defineConfig, devices } from '@playwright/test';

export default defineConfig({
  testDir: './tests',
  fullyParallel: true,
  forbidOnly: !!process.env.CI,
  retries: process.env.CI ? 2 : 0,
  workers: process.env.CI ? 4 : undefined,
  // Both reporters, so Jenkins consumes results without editing this file
  // (FR-AUT-08). The JUnit path is where the pipeline looks for test results.
  reporter: [
    ['html', { outputFolder: 'playwright-report', open: 'never' }],
    ['junit', { outputFile: 'test-results/junit.xml' }],
    ['list'],
  ],
  use: {
    // Environment-driven. No environment-specific URL is ever generated as a
    // literal (FR-AUT-07).
    baseURL: process.env.TAAS_BASE_URL,
    trace: 'on-first-retry',
    screenshot: 'only-on-failure',
    video: 'retain-on-failure',
    actionTimeout: Number(process.env.TAAS_ACTION_TIMEOUT_MS ?? 10000),
    navigationTimeout: Number(process.env.TAAS_NAVIGATION_TIMEOUT_MS ?? 30000),
  },
  expect: {
    timeout: Number(process.env.TAAS_EXPECT_TIMEOUT_MS ?? 5000),
  },
  projects: [
    {
      name: 'ui',
      testMatch: /.*\.spec\.ts/,
      use: { ...devices['Desktop Chrome'] },
    },
    {
      name: 'api',
      testMatch: /.*\.api\.spec\.ts/,
    },
  ],
});
